# Models

This folder contains strongly-typed data models that are shared across domain layers in VORTEX.

Models here are business objects, data-holder types, and any shared class structures distinct from interface-only contracts.